// Binary search algorithm in Rust

fn main() {
    let arr = [2, 3, 4, 10, 40];
    let x = 10;
    binary_search(arr, x);
}

fn binary_search(arr: [i32; 5], x: i32) {
    // your code goes here
}

// Output:
// Element found at index 3
